-- @statement
CREATE OR REPLACE VIEW V_TYPES_&run_id AS
SELECT ID, N_VAL, V_TEXT FROM T_TYPES_&run_id
-- @statement
CREATE INDEX IX_TYPES_&run_id ON T_TYPES_&run_id(V_TEXT)
-- @statement
CREATE SEQUENCE SEQ_W4_&run_id START WITH 100 INCREMENT BY 1
-- @statement
CREATE OR REPLACE TRIGGER TRG_CHILD_AUDIT_&run_id
AFTER INSERT ON T_CHILD_&run_id FOR EACH ROW
BEGIN
    INSERT INTO T_CHILD_AUD_&run_id(CHILD_ID) VALUES (:NEW.ID);
END;
-- @statement
CREATE OR REPLACE TYPE TY_POINT_&run_id AS OBJECT (
    X NUMBER,
    Y NUMBER,
    MEMBER FUNCTION NORM2 RETURN NUMBER
);
-- @statement
CREATE OR REPLACE TYPE BODY TY_POINT_&run_id AS
    MEMBER FUNCTION NORM2 RETURN NUMBER IS
    BEGIN
        RETURN X * X + Y * Y;
    END;
END;
