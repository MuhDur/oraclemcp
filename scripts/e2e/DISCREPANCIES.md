# E2E Harness Discrepancies

No accepted divergences.

Accepted future divergences must use sequential `DISC-NNN` identifiers, name the
affected script or test case, state `ACCEPTED`, `INVESTIGATING`, or `WILL-FIX`,
and be represented as XFAIL in the generated report. Do not hide known
divergences as skipped coverage.
