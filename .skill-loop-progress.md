# Skill Loop Progress
# Skill: extreme-software-optimization (re-profiling between rounds)
# Target: oraclemcp hot paths (classifier / serializer / lane+protocol)
# Total Passes: 7 (stop early on 2 consecutive zero-change or gains flatline)
# Started: 2026-07-02

## Status: COMPLETE — converged. 2 proven wins (Pass 1 -13%, Pass 2 -20%); LTO rejected (Pass 3).
## Loop stopped per skill stop-condition (remaining targets are risk/confirmation-gated; operator
## declined pushing into them). Operator confirmed: push the wins; stop the loop.

## Supersedes
Replaces the COMPLETED 2026-06-15/16 multi-skill campaign (record preserved in git
@54cc5c8; evidence under tests/artifacts/perf/2026-06-15-campaign-*). That campaign
concluded in-repo optimizations were near steady-state and rated the classifier "not
a hotspot" from WALL-CLOCK timing (~14us/stmt). This campaign uses CALLGRIND
attribution and found the allow-list SHA-256 dead-work that wall-clock missed.

## Realism (do not overclaim)
The classifier is us-scale; a live Oracle round-trip is ms-scale (p95 ~54ms cold,
~25-75ms steady). These classifier/serializer wins are REAL, FREE, and PROVEN-safe,
but their end-to-end impact is largest on classify-only paths (oracle_preview_sql,
rejected/forbidden statements that never reach the DB, high-throughput batch
classification), not on a single DB-bound query. Report both the micro-% and this
context. The genuinely dominant real-world costs (cold connect ~144ms) are
product/driver-level and out of scope, as the prior campaign already found.

## Invariants (every pass MUST honor)
- `#![forbid(unsafe_code)]`, pure-Rust, no new C deps.
- Fail-closed classifier behavior MUST stay byte-identical: run `cargo test -p oraclemcp-guard`
  (114 behavior tests) AND spot-check the differential fuzz seeds; verdicts must not change.
- One lever per pass. Re-measure the relevant baseline before/after. Score >= 2.0 only.
- Isomorphism proof required per change. No behavior change "while we're here".

## Baselines (this host: EPYC 7713, release profile)
- Classifier: 13,740 ns/stmt (pre-loop) -> 11,921 ns/stmt after Pass 1 (-13.2%).
- Serializer (page_serialization): TBD (criterion).
- Measure: `cargo test -p oraclemcp-guard --release --test perf_classifier -- --ignored --nocapture`
  and `cargo bench -p oraclemcp-db --bench page_serialization`.

## Missions
1. Classifier allow-list SHA-256 fast-path skip (empty allow_list) — DONE (-13.2%).
2. Classifier `canonical_marker_scan` allocation reduction (Vec<String>+join -> single buffer).
3. Classifier double-tokenization dedup (`analyze_batch` Tokenizer + `parse_sql` re-tokenize).
4. Serializer per-cell allocations (`name.clone()` per cell, numeric `to_owned`) — profile first.
5. Lane/protocol: `block_on_lane_bridge` runtime-per-request + `params.clone()` dispatch clones.
6. Build-config experiment: `lto="thin"` + `codegen-units=1` on release (measured tradeoff).
7. Final re-profile + steady-state/completeness check.

## Completed Passes
### Pass 1 — Classifier allow-list SHA-256 fast-path — 2026-07-02 — PRODUCTIVE
- Files: crates/oraclemcp-guard/src/classifier.rs (Lever 1), Cargo.toml (release-perf profile),
  crates/oraclemcp-guard/tests/perf_classifier.rs (env-configurable iters for callgrind).
- Change: guard allow-list lookup with `!allow_list.is_empty() &&` so empty (default) allow-lists
  skip normalize+SHA-256+hex. Callgrind confirmed sha256/hex eliminated from the default path.
- Result: 13,740 -> 11,921 ns/stmt (-13.2%); 114 guard tests green; behavior provably identical.
- Commit: f14eea9

### Pass 2 — Serializer `canonicalize_datetime` single-pass — 2026-07-02 — PRODUCTIVE
- Files: crates/oraclemcp-db/src/serialize.rs.
- Ranked #1 (`name.clone()` per cell) REJECTED: serde_json Map key is an owned String and
  rows are borrowed, so removing the clone needs a `serialize_row(&OracleRow)` signature change
  = out-of-scope/one-lever violation. Took next-best serializer-local lever.
- Change: `canonicalize_datetime` 3 allocations -> 1 single-pass buffer; runs on every
  DATE/TIMESTAMP cell. Byte-identical output (proven + 33-case differential test vs the
  original three-pass reference).
- Result: page_serialization -19% to -22% (10/200/1000 rows, criterion p<0.05); 212 db tests
  + all golden/integration suites green.
- Commit: 94a5847

### Pass 3 — Build-config LTO experiment — 2026-07-02 — REJECTED (null result)
- Measured `release-lto` (thin LTO + codegen-units=1): classifier 11,921 -> 11,586 ns/stmt (-2.8%).
- Cost: full-workspace LTO relink adds minutes to the CI release job AND every downstream
  `cargo install oraclemcp` source build.
- Verdict: net-negative for an I/O-bound server (DB round-trips are ms; a ~3% CPU gain is noise).
  NOT adopted; experiment profile reverted. No code change.

## Remaining targets — deferred to operator (stop condition: needs confirmation)
- Mission 3 (classifier double-tokenization dedup): ~10-20% micro but SECURITY-CRITICAL (fail-closed
  guard) and us-absolute. Touching it for us-gains needs differential-fuzz sign-off + operator OK.
- Mission 5 (block_on_lane_bridge runtime-per-request): highest real-world relevance (per-request,
  HTTP serving path incl. lightweight calls) BUT architecturally risky (!Send dispatch; the prior
  campaign's deadlock audit deliberately left the block-on bridges with "safe arguments").
  Warrants a measured feasibility spike + operator review, not an autonomous loop refactor.

## Campaign summary
Productive: Pass 1 classifier -13.2%, Pass 2 serializer -20% (both proven byte-identical, committed).
Rejected: Pass 3 LTO (net-negative). Converged on SAFE in-repo wins; remaining are risk/confirmation-gated.

# ============================================================================
# Campaign 2: Isomorphic refactor — simplify + de-monolithize (SAFE changes only)
# Skill: simplify-and-refactor-code-isomorphically + de-monolithize-your-codebase-isomorphically
# Started 2026-07-02. Authorized to commit+push. Behavior MUST stay byte-identical.
# ============================================================================
## Census (2026-07-02): http.rs 11,495 (B1 god-file, security surface); main.rs 6,744;
## dispatch/mod.rs 6,280; connection.rs 4,751; doctor.rs 2,988; server.rs 2,478;
## classifier.rs 2,400 (security-critical, LEAVE ALONE). AI-slop LOW (0 orphan files, 1 TODO).
## Invariants: forbid(unsafe); don't weaken the fail-closed guard; Edit-only (no rewrites/codemods);
## no file deletion without operator OK; one lever per commit; full-workspace tests + goldens gate.
## Missions: 4=simplify safe dedup; 5=de-monolith http.rs (façade-preserving extraction OR plan);
## 6=de-monolith next god-file if a clean seam; 7=convergence/stop.

## Status (Campaign 2): IN PROGRESS — Pass 4 pushed; Pass 5 (http.rs) = PLAN-ONLY (beaded); Pass 6 (dispatch/mod.rs) delegated.

### Pass 5 — De-monolith http.rs (11.5k B1 god-file) — 2026-07-02 — PLAN-ONLY (correct outcome)
- Clean seam found (request_target cluster 6714-6773; trial extraction built byte-identical) BUT
  BLOCKED: http.rs's source PATH + TEXT are a tested contract — concurrency_contract.rs include_str!,
  dashboard_e2e.rs 8 source-text scanners, path-pins in scripts/docs. http.rs->http/mod.rs crosses the
  tests/ boundary => not a conservative autonomous split. No code changed; tree pristine.
- Deliverable: decomposition PLAN (seam map + target http/ structure + extraction order) filed as bead
  `oraclemcp-demonolith-http-qyqs` (Step 1 = coordinated path-pin bead; then request_target/serve/sse/
  config/stores/operator/tests, each facade-preserving + full gate). No module statics (no B5).
- Verdict: de-monolithizing the security surface is a deliberate reviewed change, not an autonomous pass.

### Pass 6 — De-monolith dispatch/mod.rs: extract metadata_cache_key leaf — 2026-07-02 — PRODUCTIVE
- dispatch/mod.rs is NOT contract-pinned (the include_str! scans dispatch/tests.rs, untouched) and is
  already a directory module -> a split is a pure sibling-file addition (no path-move blocker).
- Moved the metadata-cache-key derivation cluster (metadata_cache_key_json + 4 pure helpers, former
  lines 4849-4888) verbatim into new crates/oraclemcp/src/dispatch/metadata_cache_key.rs (59 lines).
  Pure serialization/hashing to partition the metadata cache by DB/user/schema identity — makes NO
  auth/guard/classifier/confirmation/gate decision, touches NO dispatcher state/connection/grant.
  Byte-identical (name-anchored diff). Only change: fn -> pub(super) fn (rustfmt wraps the signature).
  mod.rs 6280 -> 6241; new focused 59-line module.
- Gate: cargo build+test --workspace = 1256 passed / 1 failed (pwsh env only) = identical to baseline;
  clippy -p oraclemcp clean; goldens unchanged; forbid(unsafe) respected.
- Commit: (this commit)

## Status (Campaign 2): COMPLETE — converged.
Landed: Pass 4 simplify (-19 LOC helper dedup) + Pass 6 de-monolith (one proven safe leaf extraction).
Plan-only: Pass 5 http.rs (beaded oraclemcp-demonolith-http-qyqs). Honest finding: this is a disciplined
codebase; its monoliths are contract-pinned (http.rs source-text scanned) or safety-coupled (dispatch/
classifier), so large de-monolith work is deliberate/reviewed (captured as beads), not autonomous. All
landed changes byte-identical, full-gate green, committed+pushed.

### Pass 4 — Simplify: dictionary-completion projection dedup — 2026-07-02 — PRODUCTIVE
- File: crates/oraclemcp-core/src/server.rs (net -19 LOC: -38/+19).
- Change: extracted free fn `completion_names_from(&Value, array, field, prefix) -> Vec<String>`,
  collapsing the identical JSON-projection tail (get(array).as_array -> filter_map field ->
  str::to_owned -> unwrap_or_default -> filter_and_sort) from complete_owners / complete_object_names
  / complete_profiles. Rule-of-3 Type II parametric clone; varies only by 2 string literals.
- Isomorphic: extracted tail has NO error path (.unwrap_or_default()); the `?` on
  dispatch_resource_tool + the Ok(..) wrapper stay at each callsite; return type / ordering /
  filter_and_sort semantics unchanged. Does NOT touch guard/classifier or dispatch authorization.
- Verify: 1256 passed / 1 failed (pre-existing env-only: pwsh absent -> installer smoke) / 3 ignored,
  IDENTICAL to baseline; clippy -D warnings clean; no golden/fixture files modified.
- REJECTED this pass (rejection log): secrets.rs resolve x2 (2 instances, intentional prod/test-inject
  split, security-adjacent); lane.rs dispatch<->mcp_surface_state (2 instances, distinct LaneCommand
  variants + error helpers + future types, concurrency-critical, Risk 5); audit record.rs
  compute_entry_hash_v2/v3 (versioned hash chain, DO NOT MERGE); main.rs self_update_argv x2
  (cfg-gated platform variants); query.rs read_query/read_query_named (2 instances, async DB path,
  positional-vs-named bind semantics); intelligence.rs query_rows cluster (all #[cfg(test)] mocks);
  the two reinvented-`?` grep hits in service_lifecycle.rs (both guarded 3-arm matches, not collapsible).
