// Retained as an inert legacy path because repository policy forbids deleting files
// without an exact operator command. Test-attestation verification is maintainer
// tooling and is intentionally absent from the end-user dashboard and bundle graph.
export {};
